{
  "name": "test-changeset-version",
  "version": "1.1.9",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "version": "npx changeset version",
    "publish": "npx changeset publish"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@changesets/cli": "^2.26.2"
  }
}
